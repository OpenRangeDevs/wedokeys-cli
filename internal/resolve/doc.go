// Package resolve holds the resolution guards shared across commands: the
// all-or-nothing Check (any unresolved alias aborts unless --allow-missing) and
// AssertSingleLine (Kamal output cannot represent multi-line values).
package resolve
