// Package config loads the wdk user config (~/.wedokeys/config.yml) and the
// project config (wdk.yml, discovered by walking up from the start directory),
// and resolves the active environment from --env / WDK_ENV / KAMAL_DESTINATION.
package config
