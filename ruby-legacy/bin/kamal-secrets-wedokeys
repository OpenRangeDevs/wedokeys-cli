#!/usr/bin/env ruby
# frozen_string_literal: true

# Kamal adapter shim — forwards to `wdk kamal-fetch`
$LOAD_PATH.unshift File.expand_path("../lib", __dir__)
require "wdk/cli"

# Rewrite argv so Thor routes to the kamal_fetch command
Wdk::CLI.start([ "kamal-fetch" ] + ARGV)
